---
import DownloadLayout from "@/layouts/DownloadLayout.astro";
---
<DownloadLayout platform="macos" title="macOS" description="Choose Apple silicon for M-series Macs or Intel for older hardware.">
  <h2>macOS notes</h2><p>Refract 1.3.0 includes a signing and notarization pipeline. If macOS still blocks the first launch, open System Settings → Privacy & Security and confirm that you want to open Refract.</p><h3>Terminal install</h3><pre><code>curl -fsSL https://refractmc.net/install.sh | sh</code></pre>
</DownloadLayout>
