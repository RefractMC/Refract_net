---
import DownloadLayout from "@/layouts/DownloadLayout.astro";
---
<DownloadLayout platform="windows" title="Windows" description="The signed x64 installer is recommended for Windows 10 and 11.">
  <h2>Windows notes</h2><p>Run the installer and follow the system prompt. WebView2 is normally present on supported Windows versions; install it from Microsoft if the application cannot open.</p><p>The MSI is useful for managed or scripted environments. Signatures for versioned assets are published beside the files on GitHub.</p>
</DownloadLayout>
